[pytest]
markers =
    integration: mark tests as integration (require external services like ES/Redis)

# Exclude integration tests and debug scripts from default test runs
addopts = --ignore=scripts/integration/ --ignore=scripts/debugging/
