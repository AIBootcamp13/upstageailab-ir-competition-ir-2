# conf/experiment/prompt_tuning.yaml
# 이 파일은 여러 프롬프트 버전을 한 번에 테스트하기 위한 멀티런(multi-run) 실험을 정의합니다.

# defaults:
#   - prompts: default

# -m 플래그와 함께 실행하면, Hydra는 아래 목록의 각 설정으로 잡(job)을 실행합니다.
# 기본 설정을 사용하고, 명령줄에서 prompts.tool_description을 오버라이드합니다.