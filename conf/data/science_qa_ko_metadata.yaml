# conf/data/science_qa_ko_metadata.yaml

# =================================================================
# 데이터 경로 설정 (Data Path Configurations)
# =================================================================
# 이 파일은 프로젝트에서 사용하는 모든 데이터 파일 및 출력 경로를 관리합니다.

# --- 입력 데이터 (Input Data) ---
documents_path: "data/documents_ko_with_metadata.jsonl"       # 원본 문서 파일 경로
validation_path: "data/validation.jsonl"    # 성능 검증용 데이터셋 경로
# validation_path: "data/validation_balanced.jsonl"    # 성능 검증용 데이터셋 경로
evaluation_path: "data/eval.jsonl"           # 최종 제출물 생성을 위한 평가 데이터셋 경로

# --- 출력 데이터 (Output Data) ---
output_path: "outputs/submission_meta.csv"     # 생성된 제출 파일이 저장될 경로